---
interface Props {
  href: string;
  variant?: 'primary' | 'secondary' | 'ghost';
  download?: boolean;
  prefetch?: boolean;
  class?: string;
}
const { href, variant = 'primary', download = false, prefetch = false, class: cls = '' } = Astro.props;

const base =
  'group inline-flex items-center gap-2 rounded-full px-5 py-2.5 text-sm font-semibold transition-all duration-200 active:scale-[0.97]';
const variants = {
  primary: 'bg-accent text-surface hover:bg-accent-strong shadow-[0_4px_20px_var(--glow)]',
  secondary: 'border border-line text-ink hover:border-ink-faint hover:bg-panel',
  ghost: 'text-ink-muted hover:text-ink',
};
---

<a
  {href}
  download={download || undefined}
  data-astro-prefetch={prefetch ? 'hover' : undefined}
  class={`${base} ${variants[variant]} ${cls}`}
>
  <slot />
</a>
