---
interface Props {
  class?: string;
}
const { class: cls = '' } = Astro.props;
---

<span
  class={`border-line bg-panel text-ink-muted inline-flex items-center rounded-full border px-3 py-1 text-xs font-medium ${cls}`}
>
  <slot />
</span>
