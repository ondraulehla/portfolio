---
interface Props {
  class?: string;
}
const { class: cls = '' } = Astro.props;
---

<span
  class={`border-line text-ink-muted inline-flex items-center border px-2.5 py-1 font-mono text-xs ${cls}`}
>
  <slot />
</span>
